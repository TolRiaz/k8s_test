apiVersion: extensions/v1beta1
kind: Deployment
metadata:
    name: udp-client
spec:
    replicas: 4
    template:
        metadata:
            name: udp-client
            labels:
                app: udp-client
        spec:
            containers:
            - name: test
              image: tolriaz/iperf
              command: ["/bin/bash"]
              args: ["-c","cd /usr/share/nginx/html/ && ./client_iperf.sh 220.149.13.191 20 30000"]
              ports:
              - name: http-port
                containerPort: 80
              resources:
                  requests:
                      cpu: 200m
